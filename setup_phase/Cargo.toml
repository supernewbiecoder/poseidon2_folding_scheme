[package]
name = "setup_phase"
version = "0.1.0"
edition = "2021"

[dependencies]
# Thư viện mã hóa và xử lý file khóa
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3" # Dùng để serialize cặp khóa ra dạng nhị phân (.bin) tối ưu dung lượng

# Crate lõi dùng chung trong Workspace
core = { path = "../core" }